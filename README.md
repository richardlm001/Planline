# Planline
Dead simple, single project roadmap planner: drag and resize tasks on a clean Gantt timeline, link finish to start dependencies, and auto shift downstream work. Fully frontend, easy to fork and run locally
